# Workflows — `pm` domain

Markdown workflows for **project management**: planning, roadmap, and coordination.

Currently, project management needs are covered by the `/ticket` workflow in the dev domain.

| `/ticket` | Run the **ticket** workflow ([`../dev/ticket.md`](/workflows/dev/ticket.md)) — Kanban; layout and phases in that file (skills under [`skills/`](/skills/)) |

Parent index: [`workflows/README.md`](/workflows/README.md).
