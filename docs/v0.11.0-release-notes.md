# v0.11.0 — Scope freeze and public trust cleanup

## Product identity

- Public positioning: **lightweight, markdown-first workflow kit** for AI coding agents (not a framework, harness OS, or runtime platform).
- README adds **Why not a framework?**, **Known limitations**, and **Release discipline** sections.

## Provider scope

- **Active:** Claude Code (primary), Cursor (secondary), Codex (experimental), Gemini (experimental).
- **Removed from active scope:** OpenCode — no wizard/install-runtime/matrix install path; legacy uninstall via `aih.sh uninstall --runtime opencode` only.
- Historical CHANGELOG and dogfood reports may still mention OpenCode.

## Verification

- `node validate.js` — maintainer contract check.
- `npm test` — install/CLI/provider scope regression tests.

## Notes

- `runtime/opencode/` may remain in the repo for reference; it is not an active install target.
## Step 3 — Dogfood demo

- [examples/dogfood-tiny-node-api](../examples/dogfood-tiny-node-api): tiny Node API + full `.harness/` loop (goal → plan → verify → ship) with `status: passed` backed by real `npm test` output.
## Step 4 — CI trust and public demo polish

- [.github/workflows/ci.yml](../.github/workflows/ci.yml): `node validate.js`, `npm test`, dogfood `npm test` on Node 20.
- README: CI badge, [Demo](../README.md#demo) section, link to [TRANSCRIPT.md](../examples/dogfood-tiny-node-api/TRANSCRIPT.md).
- Replaced legacy `validate.yml`-only workflow with unified CI job.
