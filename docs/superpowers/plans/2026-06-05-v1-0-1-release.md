# v1.0.1 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prepare a clean `v1.0.1` release with aligned version metadata, release notes, changelog, and verification for publish.

**Architecture:** Keep the release pass narrow: update only the package version, latest-release references, release notes, and release checklist. Add a focused regression test so future patch bumps do not miss one of the visible release surfaces.

**Tech Stack:** Markdown, React, TypeScript, Node test runner, npm packaging

---

### Task 1: Add release metadata regression coverage

**Files:**
- Create: `test/release-version.test.js`
- Test: `test/release-version.test.js`

- [ ] **Step 1: Write the failing test**

```js
const { test } = require("node:test");
const assert = require("node:assert/strict");
const fs = require("node:fs");
const path = require("node:path");

const repoRoot = path.resolve(__dirname, "..");
const version = "1.0.1";

test("release-facing files point at v1.0.1", () => {
  const pkg = JSON.parse(fs.readFileSync(path.join(repoRoot, "package.json"), "utf8"));
  const lock = JSON.parse(fs.readFileSync(path.join(repoRoot, "package-lock.json"), "utf8"));
  const readme = fs.readFileSync(path.join(repoRoot, "README.md"), "utf8");
  const docsIndex = fs.readFileSync(path.join(repoRoot, "docs", "README.md"), "utf8");
  const notes = fs.readFileSync(path.join(repoRoot, "docs", "v1.0.1-release-notes.md"), "utf8");
  const hero = fs.readFileSync(path.join(repoRoot, "site", "src", "components", "Hero.tsx"), "utf8");
  const footer = fs.readFileSync(path.join(repoRoot, "site", "src", "components", "Footer.tsx"), "utf8");

  assert.equal(pkg.version, version);
  assert.equal(lock.version, version);
  assert.equal(lock.packages[""].version, version);
  assert.match(readme, /v1\\.0\\.1/);
  assert.match(readme, /docs\\/v1\\.0\\.1-release-notes\\.md/);
  assert.match(docsIndex, /v1\\.0\\.1/);
  assert.match(notes, /# v1\\.0\\.1/);
  assert.match(hero, /v1\\.0\\.1/);
  assert.match(footer, /v1\\.0\\.1/);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test --test-concurrency=1 --experimental-test-isolation=none test/release-version.test.js`
Expected: FAIL because the repo is still on `1.0.0`.

- [ ] **Step 3: Write minimal implementation**

```text
Update package metadata, docs metadata, and site version strings to v1.0.1, and create docs/v1.0.1-release-notes.md.
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test --test-concurrency=1 --experimental-test-isolation=none test/release-version.test.js`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json README.md docs/README.md docs/v1.0.1-release-notes.md site/src/components/Hero.tsx site/src/components/Footer.tsx test/release-version.test.js
git commit -m "chore: prepare v1.0.1 release metadata"
```

### Task 2: Write changelog and release docs

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `docs/release-checklist.md`
- Modify: `docs/npm-publish.md`

- [ ] **Step 1: Add the v1.0.1 changelog entry**

```text
Summarize README polish, landing-page refresh, walkthrough integration, and release-alignment work under a dated 1.0.1 heading.
```

- [ ] **Step 2: Update release process docs for v1.0.1**

```text
Add the manual final-command block for v1.0.1 and ensure publish guidance matches the chosen patch-release flow.
```

- [ ] **Step 3: Commit**

```bash
git add CHANGELOG.md docs/release-checklist.md docs/npm-publish.md
git commit -m "docs: add v1.0.1 release notes and checklist"
```

### Task 3: Run release verification

**Files:**
- Test: `test/release-version.test.js`

- [ ] **Step 1: Run targeted release metadata test**

Run: `node --test --test-concurrency=1 --experimental-test-isolation=none test/release-version.test.js`
Expected: PASS

- [ ] **Step 2: Run repository tests**

Run: `npm test`
Expected: PASS

- [ ] **Step 3: Run site build**

Run: `npm run build`
Working directory: `site/`
Expected: PASS

- [ ] **Step 4: Run packaging dry-run**

Run: `npm pack --dry-run`
Expected: tarball is created in dry-run output and excludes `test/` and `examples/`.

- [ ] **Step 5: Review final release diff**

Run: `git diff -- package.json package-lock.json README.md CHANGELOG.md docs/README.md docs/v1.0.1-release-notes.md docs/release-checklist.md docs/npm-publish.md site/src/components/Hero.tsx site/src/components/Footer.tsx site/src/components/ProviderCards.tsx test/release-version.test.js`
Expected: release changes are scoped to `1.0.1` metadata, notes, and current polish work.
