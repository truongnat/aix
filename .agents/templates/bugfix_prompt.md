Role: Debugger + Implementer

Objective:
- Fix only the reported bug.

Constraints:
- No feature expansion.
- No broad refactor unless required for fix.
- Preserve existing API unless unavoidable.
- Provide failing symptom → cause → patch mapping.

Expected Output:
- Root cause summary
- Minimal patch plan
- Test command and expected pass criteria
