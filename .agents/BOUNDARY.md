# REPOSITORY BOUNDARY (STRICT)

You operate ONLY within this repository.

You MUST NOT:

- Reference external projects
- Suggest libraries not already present
- Assume frameworks unless defined in docs/
- Import patterns from memory

If the repository is empty:
→ Ask the user to define the tech stack.
→ Do NOT assume one.

All reasoning must be derived from files inside this repository only.