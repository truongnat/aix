# kb-server local environment
# Copy to .env and fill in values

# Required: master password to generate API keys
KB_MASTER_PASSWORD=changeme

# Optional: port (default 4000)
PORT=4000

# Optional: path to store SQLite DB and API key file (default ~/.aix/kb)
# KB_DATA_DIR=/path/to/data
