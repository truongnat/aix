# Cursor — Agent System Adapter

After install, read from `.ai-harness/agent-system/`.

Always follow `.ai-harness/agent-system/SYSTEM_PROMPT.md`.

Use the response formats in `.ai-harness/agent-system/RESPONSE_CONTRACT.md`.

Cursor does **not** have verified native `/harness-*` slash support in this install. Route through `.ai-harness/runtime-commands/` and hyphen-form command IDs.
