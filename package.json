{
  "name": "aix-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "aix — AI Engineering Platform (TypeScript monorepo)",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "check": "turbo run check",
    "clean": "turbo run clean",
    "doctor": "node packages/cli/dist/index.js doctor"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}