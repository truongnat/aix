# Skill: next_steps

```json
{
  "name": "next_steps",
  "domain": "agent",
  "executor": "ollama",
  "model": "qwen3:8b",
  "temperature": 0.1
}
```

Add more tasks to the workflow dynamically.
Input should be a newline-separated list of tasks.

{{input}}
